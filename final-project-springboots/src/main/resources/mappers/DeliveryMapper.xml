<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--	
	마이바티스 참고 사이트
	  - http://blog.mybatis.org/
	  - http://www.mybatis.org/mybatis-3/ko/index.html
	  
	@Mapper 애노테이션이 적용된 인터페이스와 맵퍼 XML파일은
	namespace 속성으로 연결되기 때문에 맵퍼 XML의 namespace를
	지정할 때 @Mapper 애노테이션이 적용된 맵퍼 인터페이스의
	완전한 클래스 이름(패키지를 포함한 이름)을 지정하면 된다.   
-->
<mapper namespace="com.springbootstudy.bbs.mapper.DeliveryMapper">
	<insert id="insertDelivery" useGeneratedKeys="true" keyProperty="deliveryIdx"> INSERT INTO
		delivery ( pay_idx, bid_idx, courier_company, tracking_number, shipped_at, delivery_status )
		VALUES ( #{payIdx}, #{bidIdx}, #{courierCompany}, #{trackingNumber}, NOW(), 'SHIPPING' ) </insert>

	<!-- 배송 상태 변경 -->
	<update id="updateDeliveryStatus"> UPDATE delivery SET delivery_status = #{deliveryStatus},
		delivered_at = CASE WHEN #{deliveryStatus} = 'DELIVERED' THEN NOW() ELSE delivered_at END WHERE
		bid_idx = #{bidIdx} </update>

	<!-- bid_idx로 배송 정보 조회 -->
	<select id="findDeliveryByBidIdx" resultType="com.springbootstudy.bbs.domain.DeliveryVO"> SELECT *
		FROM delivery WHERE bid_idx = #{bidIdx} </select>

	<!-- pay_idx로 배송 정보 조회 -->
	<select id="findDeliveryByPayIdx" resultType="com.springbootstudy.bbs.domain.DeliveryVO"> SELECT *
		FROM delivery WHERE pay_idx = #{payIdx} </select>
</mapper>	